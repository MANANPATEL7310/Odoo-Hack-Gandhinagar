[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm db:generate && pnpm build --filter @template/api"

[deploy]
startCommand = "node apps/api/dist/server.js"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 100
